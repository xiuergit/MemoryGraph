# MemoryGraph Continue 项目规则

## 上下文

- 项目：MemoryGraph 家庭私有记忆系统
- 阶段：Photo → JSON → SQLite 索引 → CLI 问答（Web/Event 未做）
- Mac：Python（`hub/shared/`、`tools/`）
- iOS：Swift（`apps/ios/`，待开发；当前主路径为导出原图到 Mac）

## 改代码流程

1. 修改前先读取相关源文件
2. 一次只做一个明确任务
3. 只改用户要求的文件，保持最小 diff
4. 改完后说明如何验证：
   - 批量：`python tools/photo2json/main.py`
   - 索引：`python tools/memory_index/main.py sync`
   - 问答：`python tools/memory_ask/main.py "测试问题"`
   - Hub：`python hub/mac_server/main.py`

## 不可违反的约束

- JSON Schema 以 `schemas/photo.v1.json` 为准，字段名不可随意增删改
- 唯一图像 AI 入口：`analyze_image()` in `hub/shared/analyzer.py`
- JSON 为唯一数据源；SQLite 仅索引，可 sync 重建
- 原图存 `data/photos/`，JSON 存 `data/memory/`
- 单张失败不中断批处理
- 照片不上传云端 AI；逆地理仅发 GPS 坐标

## 关键文件

- `schemas/photo.v1.json` — 协议定义
- `hub/shared/analyzer.py` — 图像 AI 入口
- `hub/shared/vision.py` — Ollama 视觉模型
- `hub/shared/face.py` — 人脸识别
- `hub/shared/index_db.py` — SQLite 同步
- `hub/shared/agent.py` — CLI 问答
- `hub/shared/schema.py` — Python Schema 实现
- `tools/photo2json/processor.py` — Mac 批量流水线
- `hub/mac_server/server.py` — iOS 上传接口
- `docs/operations.md` — 日常操作手册
- `docs/architecture.md` — 架构说明
- `AGENTS.md` — 完整协作约束

## Schema 要点

- `schema_version`：当前 `"1.1"`（含 `location_coords`）
- `location`：可读地名；`location_coords`：EXIF GPS，不覆盖
- `timestamp`：优先 EXIF，无则空字符串
- `device_id`：`iphone` / `mac` 等
- `source.path/width/height`：真实读取，不由 AI 返回
- `manual_edit: true` 时批处理不覆盖
- AI 字段由 `analyze_image()` 返回
